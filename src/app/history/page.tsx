import { HistoryList } from '@/components/history-list';

export default function HistoryPage() {
    return (
        <main className="flex-1 w-full max-w-7xl mx-auto px-4 py-8 overflow-y-auto">
            <HistoryList />
        </main>
    );
}
